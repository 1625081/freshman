<% @search_ids = [] %>
<% for post in results do @search_ids << post.id end %>
<%= form_tag more_posts_path,remote:true do %>
  <%= hidden_field_tag(:search_ids,@search_ids) %>
  <button type="submit" class="fluid green ui bottom attached button">点击加载更多</button>
<% end %>
